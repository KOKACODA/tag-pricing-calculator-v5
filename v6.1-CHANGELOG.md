# KOKALabel报价系统 v6.1 — 版本总结文档

> 更新日期：2026-08-14
> 仓库：KOKACODA/tag-pricing-calculator-v5
> 部署：https://tag-pricing-calculator-v5.pages.dev

---

## 一、v6.1 核心变更

### 1. 导入新报价表为默认数据库

`DEFAULT_PAPER_CONFIG` 与 `DEFAULT_CRAFT_CONFIG` 全面更新为 2026-08-14 新版报价表数据：

- 共 10 张 Sheet（350铜版纸 / 400铜版纸 / 702铜版纸 / 606米白卡 / 500白卡纸 / 海成专用 / 700布纹纸 / 600牛皮纸 / 40C棉麻布 / 602米白卡）
- 每张 Sheet 的规格（代码 × 档位）价格矩阵、折扣系数、工艺价格均按新表更新
- 新增 602米白卡（paper10）及其工艺配置

### 2. 直接系数改为每个 Sheet 单独配置

每张纸（Sheet）拥有独立的 `directCoeff` 字段：

```js
{
  "directCoeff": {
    "tierRules": [
      { "tierMin": 1000,  "tierMax": 4000,      "max": 1.6,  "min": 1.5  },
      { "tierMin": 5000,  "tierMax": 10000,     "max": 1.5,  "min": 1.45 },
      { "tierMin": 20000, "tierMax": Infinity,  "max": 1.45, "min": 1.4  }
    ]
  }
}
```

- **702铜版纸** 已配置专属档位规则（见上表）
- 其余 Sheet 提供 `directCoeff` 占位模板（空 `tierRules`），后续修改时只需填入规则
- `getDirectCoeffsForTier(tier, paper)` 优先使用该 Sheet 的 `directCoeff.tierRules`，未配置时回退到全局 `DIRECT_COEFF_TIER_RULES`

### 3. 702铜版纸 直接系数档位规则

| 档位范围 | 最高系数 | 最低系数 | 三档插值结果 |
|---|---|---|---|
| 1000 ~ 4000 张 | 1.6 | 1.5 | 1.6 / 1.55 / 1.5 |
| 5000 ~ 10000 张 | 1.5 | 1.45 | 1.5 / 1.48 / 1.45 |
| 20000 张以上 | 1.45 | 1.4 | 1.45 / 1.42 / 1.4 |
| 500 张及以下 | 使用等级设置值 | 使用等级设置值 | 1.5 / 1.45 / 1.4 |

### 4. Excel 模板 / 导出 / 导入支持直接系数

- **模板**：模板改为基于 `DEFAULT_PAPER_CONFIG` + `DEFAULT_CRAFT_CONFIG` 动态生成（10 张 Sheet，含 602米白卡），所有 Sheet 均含「直接系数档位规则」行，格式 `最低张数-最高张数:最高系数/最低系数`，多条用 `;` 分隔；702铜版纸 已填好示例值，其余留空待填
- **导出**：导出 Excel 时自动写入「直接系数档位规则」行，保证导入导出可往返
- **导入**：重新导入报价表时，若 Sheet 填写了「直接系数档位规则」则自动解析；未填写时匹配默认简称则继承默认配置（如 702铜版纸），否则提供占位

### 5. 直接系数模式不再触发纸张折后价

- 直接系数模式下使用原价（不打折），系数已包含利润
- 报价结果隐藏「纸张折后价合计」行
- 标准报价模式仍正常显示折扣前/折后价

### 6. 首页默认打开直接系数计算 + 切换 UI 调换

- `calcMode` 默认值改为 `direct`（持久化到 localStorage）
- 启动时同步隐藏吊绳/邮费输入区域
- 模式切换按钮顺序调换为「直接系数计算」在前、「标准报价」在后

---

## 二、v6.1 涉及文件

| 文件 | 变更 |
|---|---|
| `js/data.js` | 新报价表默认库、所有 Sheet 增加 `directCoeff`、迁移逻辑更新 |
| `js/app.js` | 直接系数模式隐藏折后价、默认模式 direct、导入/导出/模板支持直接系数 |
| `index.html` | 模式切换 UI 调换、版本号 v6.1 |
| `README.md` / `CHANGELOG.md` | 版本记录更新 |

---

## 三、后续维护说明

1. **修改某 Sheet 的直接系数**：推荐直接修改 Excel 中该 Sheet 的「直接系数档位规则」行后重新导入；也可在个人主页全局档位规则中调整（影响未配置的 Sheet）
2. **重新导入报价表**：导入时 Sheet 简称匹配默认配置则继承对应 `directCoeff`，未匹配则提供占位
3. **版本号同步**：修改版本号时，index.html（title + CSS/JS 引用 `?v=X.X`）、app.js、data.js 需同步更新
