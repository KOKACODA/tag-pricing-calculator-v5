# KOKALabel报价系统 v6.2 — 版本总结文档

> 版本：v6.2 · 日期：2026-08-14
> 核心主题：**直接系数改为「只读报价表表格」+ 三行格式（直接系数档位 / 最高倍数 / 最低倍数）**

---

## 一、v6.2 核心变更

### 1. 直接系数改为只读报价表表格（取消全局设置）

- **取消个人主页「直接系数设置」卡片**：不再支持在个人主页编辑直接系数等级或批量档位规则。
- **保留「临时直接系数」输入**：报价结果页的临时系数快速设置仍保留，仅影响当前显示，不写入任何持久化设置。
- **直接系数只读取报价表（Excel）每个 Sheet 的表格字段**：`直接系数档位 / 最高倍数 / 最低倍数` 三行数据。

### 2. 三行格式（与报价表表格一致）

每张纸（Sheet）的 `directCoeff` 改为数组格式：

```json
{
  "tiers": [500, 1000, 2000, 2500, 3000, 5000, 10000, 20000, 50000],
  "max":  [1.3, 1.3, 1.3, 1.3, 1.3, 1.2, 1.2, 1.2, 1.2],
  "min":  [1.25, 1.25, 1.25, 1.25, 1.25, 1.15, 1.15, 1.15, 1.15]
}
```

- `直接系数档位`：批量张数档位。
- `最高倍数`：该档位的最高系数（对应普通客户）。
- `最低倍数`：该档位的最低系数（对应大客户）。
- 中间等级（优质客户）按最高/最低等差插值。
- 无直接系数的 Sheet：`directCoeff` 为 `null`，按标准报价计算。

### 3. 计算规则

- **有直接系数的 Sheet**：直接系数模式下使用原价（**不再计算折扣**），报价 = 成本 × 表格档位系数。
- **无直接系数的 Sheet**：按标准报价计算（乘折扣系数），使用客户建议价格 · 毛利系数。
- 直接系数模式下隐藏「纸张折后价合计」行、吊绳费用行、邮费行。

### 4. 报价表组查询页新增直接系数显示

- 查询页工具栏新增 `直接系数档位` 徽章，展示当前 Sheet 的档位与最高/最低系数。
- 无直接系数的 Sheet 不显示该徽章。

### 5. 导出 / 导入 / 模板全面支持三行格式

- **导出**：每个 Sheet 写入「直接系数档位 / 最高倍数 / 最低倍数」三行。
- **模板**：所有 Sheet 均含三行；有直接系数的 Sheet 填实际值，无直接系数的 Sheet 提供占位档位行（最高/最低留空），方便填写后重新导入。
- **导入**：自动解析三行；三行均填有值且档位数量一致 → 有效直接系数；仅档位占位或无任何行 → `null`（按标准报价）。

### 6. 默认报价表更新（新表格数据）

- `DEFAULT_PAPER_CONFIG` 全面更新为最新报价表数据。
- 350铜版纸（paper1）直接系数示例：档位 500~50000，500-3000 张 最高1.3/最低1.25，5000 张起 最高1.2/最低1.15。
- 702铜版纸（paper3）保持专属规则：1000-4000张 最高1.6/最低1.5；5000-10000张 最高1.5/最低1.45；20000张以上 最高1.45/最低1.4。

---

## 二、v6.2 涉及文件

| 文件 | 变更 |
|------|------|
| `js/data.js` | 默认报价表数据更新为三行格式；`DIRECT_COEFF_LEVELS` 改为固定展示名称；新增 v6.2 迁移逻辑 |
| `js/app.js` | `paperHasDirectCoeff` / `getDirectCoeffsForTier` 读取表格三行；导出/导入/模板三行格式；查询页直接系数显示；移除全局直接系数设置函数 |
| `index.html` | 移除个人主页「直接系数设置」卡片，改为「直接系数说明」；查询页新增直接系数徽章；版本号 v6.2 |
| `css/style.css` | 直接系数徽章样式；版本号 v6.2 |
| `README.md` / `CHANGELOG.md` / `HANDOFF-v6.0.md` / `AGENT-INSTRUCTIONS.md` | 版本号与变更说明同步 v6.2 |

---

## 三、后续维护说明

1. **修改直接系数**：直接编辑报价表 Excel 的「直接系数档位 / 最高倍数 / 最低倍数」三行，然后重新导入。
2. **无直接系数的 Sheet**：在报价表模板中填写三行（档位 + 最高/最低倍数）后重新导入即可启用直接系数。
3. **版本号同步**：修改版本号时，index.html（title + CSS/JS 引用 `?v=X.X`）、app.js、data.js 需同步更新。
