# KOKALabel报价系统 v6.4 — 版本总结文档

> 版本：v6.4 · 日期：2026-08-14
> 核心主题：**去掉纸张层面折扣（discount 不再参与计算），所有模式下纸张按原价计算，解决"重复打折"问题**

---

## 一、v6.4 核心变更

### 1. 去掉纸张层面折扣（无重复打折）

**问题**：v6.4 之前，纸张价格 = 原价 × `paper.discount`，然后最终报价再 × 客户等级系数（或直接系数），相当于双重折扣。例如 40C棉麻布原价 90，discount 0.75，普通客户 1.2 倍：
- 旧逻辑：90 × 0.75 × 1.2 = 81 元（纸张先打折，再乘毛利系数 = 重复打折）

**修复**：v6.4 起，所有模式下纸张均按原价计算，折扣只通过客户等级系数（标准模式）或直接系数（直接系数模式）体现：
- 新逻辑：90 × 1.2 = 108 元（原价 × 毛利系数 = 单次折扣）

`paper.discount` 字段仍保留（查询页显示折扣信息、导入导出兼容），但不再参与计算。

### 2. 各纸张表格单价显示原价

"各纸张尺寸 / 面积 / 代码"表格的单价列统一显示原价：
- 40C棉麻布 1000张：¥90（之前显示 ¥67.50）
- 500白卡纸 1000张：显示原价（之前显示折后价）
- 400铜版纸 1000张：显示原价（之前显示折后价）
- 以此类推...

### 3. 纸张价合计行调整

- **纸张价合计（折扣前）**：始终显示原价合计（之前也是原价，不变）
- **纸张折后价合计**：值与折扣前相同（因为不再有纸张折扣），行仍保留用于兼容
- 直接系数模式下仍隐藏折后价行（之前的逻辑）

### 4. 查询页直接系数显示统一校验

改用 `paperHasDirectCoeff` 函数判断是否显示直接系数档位信息，确保「直接系数档位 / 最高倍数 / 最低倍数」三行均非空且数量一致时才显示，与其他地方的判断逻辑保持一致。

---

## 二、v6.4 涉及文件

| 文件 | 变更 |
|------|------|
| `js/app.js` | `calculate()` 中 `baseUnitPrice` 不再乘 `paper.discount`，始终用原价；查询页直接系数显示统一用 `paperHasDirectCoeff`；版本号 v6.4 |
| `js/data.js` | 版本号 v6.4（数据结构不变，discount 字段保留但不再参与计算） |
| `index.html` | 版本号 v6.4（title + CSS/JS 引用 `?v=6.4`） |
| `README.md` | 版本号与 v6.4 改动摘要更新 |
| `CHANGELOG.md` | 新增 v6.4 更新记录 |
| `HANDOFF-v6.0.md` | 版本号与计算方式说明更新 |
| `AGENT-INSTRUCTIONS.md` | 版本号更新 |

---

## 三、后续维护说明

1. **discount 字段**：仍保留在数据结构中，查询页显示折扣信息，导入导出兼容。如需重新启用纸张折扣，修改 `calculate()` 中 `baseUnitPrice` 的计算即可。
2. **价格对比**：v6.4 起所有纸张报价基于原价计算，最终报价 = 原价合计 × 系数。与 v6.3 相比，有 discount 的纸张（如 40C棉麻布 0.75、500白卡纸 0.75、400铜版纸 0.91）最终报价会上涨。
3. **版本号同步**：修改版本号时，index.html（title + CSS/JS 引用 `?v=X.X`）、app.js、data.js 需同步更新。
