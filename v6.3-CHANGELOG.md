# KOKALabel报价系统 v6.3 — 版本总结文档

> 版本：v6.3 · 日期：2026-08-14
> 核心主题：**无直接系数的纸张在纸张设置卡片高亮提示「请切换为标准报价」**

---

## 一、v6.3 核心变更

### 1. 无直接系数纸张提示

纸张设置卡片中，无直接系数的 Sheet（如 700布纹纸、40C棉麻布）在「纸张材质」下拉框下方高亮显示提示条：

```
⚠ 该纸张无直接系数（最高/最低倍数为空），请切换为标准报价计算
```

- **内联显示，不弹窗**：提示直接显示在纸张材质下拉框旁边，无需确认。
- **高亮样式**：橙色警示条（左侧橙色竖线 + 浅橙背景），与普通提示区分。
- **切换纸张即时更新**：选择其他纸张后重新渲染，有直接系数的纸张自动隐藏提示。

### 2. 提示覆盖所有导入场景

导入其他报价表后，以下情况均显示提示：

- 无直接系数（`directCoeff` 为 `null` 或缺失）
- 直接系数最高倍数为空
- 直接系数最低倍数为空
- 档位与最高/最低数量不一致（部分档位空值被过滤）

### 3. 直接系数有效性校验增强

`paperHasDirectCoeff` 函数增强：要求「直接系数档位 / 最高倍数 / 最低倍数」三行均非空且**长度一致**才算有效，任何不完整配置均视为无直接系数，提示切换标准报价。

---

## 二、v6.3 涉及文件

| 文件 | 变更 |
|------|------|
| `js/app.js` | `renderSheets()` 新增无直接系数提示条；`paperHasDirectCoeff` 增强三行长度一致性校验；版本号 v6.3 |
| `css/style.css` | 新增 `.sheet-direct-warning` 高亮提示样式；版本号 v6.3 |
| `index.html` | 版本号 v6.3（title + CSS/JS 引用 `?v=6.3`） |
| `js/data.js` | 版本号 v6.3 |
| `README.md` / `CHANGELOG.md` / `HANDOFF-v6.0.md` / `AGENT-INSTRUCTIONS.md` | 版本号与变更说明同步 v6.3 |

---

## 三、后续维护说明

1. **无直接系数的 Sheet**：在报价表模板中填写「直接系数档位 / 最高倍数 / 最低倍数」三行后重新导入，提示自动消失。
2. **直接系数不完整**：若档位与最高/最低数量不一致（如部分档位留空），系统视为无直接系数并提示，请补全后重新导入。
3. **版本号同步**：修改版本号时，index.html（title + CSS/JS 引用 `?v=X.X`）、app.js、data.js 需同步更新。
