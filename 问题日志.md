# KOKALabel 报价系统问题日志

- 记录日期：2026-09-02
- 当前版本：v7.8
- 状态：仅记录，暂不处理

## 已确认问题

### 1. 标准报价在纸张或工艺缺价时仍可能生成正式报价

标准报价模式的 `costIncomplete` 没有完整纳入纸张和工艺的缺价状态。实测“350铜版纸、3000张”没有纸张价格时，系统仍将吊绳和邮费合计为成本 ¥50，并生成普通客户报价 ¥60。

### 2. 多纸张报价的邮费受纸张排列顺序影响

小面积邮费折扣只读取第一张纸的面积。同样两张纸仅交换排列顺序，实测邮费会从 ¥12 变为 ¥15，总成本会从 ¥137.10 变为 ¥140.10。

### 3. 面积规格在 6000mm² 边界发生代码和价格回落

硬编码映射使 5500–5999mm² 使用 `006`，正好 6000mm² 时重新匹配为 `055`，6001mm² 又回到 `006`。面积增加到 6000mm² 时可能出现价格下降。

### 4. 数据恢复和邮费导入持久化不完整

- 快照中保存了吊绳和邮费配置，但恢复快照时没有恢复这两项。
- 邮费 Excel 导入后没有写入 `localStorage`，刷新页面后可能丢失。
- 动态重建吊绳选项后，新单选按钮可能没有重新绑定实时计算事件。（已在 v7.9 修复：新增 `bindRopeEvents()`，重建后自动重新绑定）

### 5. Excel 模板逻辑与多报价表结构冲突

“下载 Excel 模板”会遍历全部 44 张默认纸张，而不是只处理当前报价表。不同报价表存在重复纸张简称，可能造成 Excel 工作表重名失败；模板中的所属小组和报价表元信息也会统一写成当前报价表。

### 6. 默认纸张设置没有真正应用到计算器

页面静态内容和说明暗示默认纸张为“702铜版纸”，实际运行时两张纸均初始化为“350铜版纸”。个人主页中的默认纸张设置主要改变报价表查询页索引，没有改变计算器内的默认纸张。

### 7. 直接系数的中间等级存在浮点舍入偏差

例如最高系数 1.30、最低系数 1.25，中间值 1.275 当前会显示为 1.27；按常见十进制四舍五入预期应为 1.28。默认配置中有多个档位受影响。

### 8. Excel/JSON 导入校验与输出转义不足

- 未完整阻止负价格、规格乱序、直接系数异常等数据。
- 档位表头中间出现空列时仅警告但仍继续导入，可能造成价格错位。
- 部分导入状态通过未转义的 `innerHTML` 输出文件内容，存在 HTML 注入面。

## 其他一致性问题

- v7.8 导出的 JSON 版本字段仍为 `6.5`。
- 恢复提示写有“43张纸”，默认数据实际为 44 张纸。
- 报价历史、云同步、客户管理、订单管理、统计报表、价格趋势和 PDF 报价单目前仍为占位功能。
- 公司名称和联系电话目前主要用于配置保存，尚未真正接入报价单输出。

