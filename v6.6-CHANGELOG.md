# KOKALabel报价系统 v6.6 — 版本总结文档

> 版本：v6.6 · 日期：2026-08-15
> 核心主题：**导入导出健壮性修复 — 工艺跨报价表隔离 + 空列/折扣系数校验 + 数据校验完整性**

## 一、修复问题清单

### 高严重度：工艺配置跨报价表覆盖

**问题**：`parsePaperExcel` 中，导入 Sheet 若简称与默认纸张相同（如"400铜版纸"），会复用默认 `paper id`（如 `paper2`）。`CRAFT_CONFIG` 以 `paper.id` 为键且跨报价表共享，新报价表的工艺直接覆盖原报价表的工艺，切换回原报价表后工艺价格全部错误，且**无任何提示**。

**修复**：导入纸张始终生成唯一 ID（`paper_import_N`），不再复用默认 `paper id`。Excel 无工艺区时，从默认配置复制同名简称纸张的工艺到新 ID 下，保持"导入后工艺可用"的既有行为。各报价表工艺完全隔离。

### 中严重度：档位表头空列价格错位

**问题**：修改表格时若档位表头中间留空列（误删列、合并单元格残留），解析时 `tierKeys` 跳过空列，但数据行仍按固定列位读取，导致价格**静默错位**。

**修复**：导入时检测表头中间空列（第一个有效档位之后出现空列且其后仍有有效档位），提示「价格可能错位，请检查后重新导入」；末尾空列不误报。

### 中严重度：折扣系数上限校验

**问题**：`discount` 只校验 `<= 0`，不校验 `> 10`。误填 15 会被接受，标准报价模式下价格放大 15 倍。

**修复**：校验范围 0~10，超出回退为 1 并提示「折扣系数无效（应为 0~10 之间）」。

### 低严重度：全量导入缺少数据校验

**问题**：`importFullData` 直接 `JSON.parse + 赋值`，未调用 `validateImportedData`，损坏/恶意 JSON 可能导致运行时错误。

**修复**：全量导入前执行 `validateImportedData`，与本地备份恢复一致。

### 低严重度：导出版本号未更新

**问题**：`exportFullData` / `exportLocalBackup` 仍写 `version: "6.0"`，实际已是 v6.5。

**修复**：版本号更新为 `6.5`。

## 二、验证结果

通过 12 项自动化验证测试：

| 验证项 | 结果 |
|--------|------|
| 模板往返导入（10 张纸规格/价格/直接系数全部正确） | ✓ |
| 导入纸张不再复用默认 ID | ✓ |
| 工艺从默认配置复制到新 ID 下 | ✓ |
| 工艺跨报价表不再覆盖（原报价表工艺保留） | ✓ |
| 新报价表工艺存于独立 ID | ✓ |
| 表头中间空列检测提示 | ✓ |
| 末尾空列不误报 | ✓ |
| 折扣系数 15 被拒绝回退为 1 | ✓ |
| 折扣系数无效提示输出 | ✓ |
| 无直接系数纸张占位回退标准模式 | ✓ |
| 直接系数保留 | ✓ |

## 三、文件变更

| 文件 | 变更 |
|------|------|
| `js/app.js` | 5 处修复：ID 分配逻辑、工艺复制、空列检测、折扣系数校验、全量导入校验、导出版本号 |
| `index.html` | 版本号 v6.6（标题 + 资源引用 + 个人主页显示） |
| `js/data.js` | 版本号 v6.6（顶部注释） |
| `css/style.css` | 版本号 v6.6（顶部注释） |
| `CHANGELOG.md` | 新增 v6.6 条目 + 补充 v5.2.1 独立条目 |
| `README.md` | 更新版本号、改动摘要、版本历史表 |
| `HANDOFF-v6.0.md` | 更新版本号、文件结构、新增 v6.6 说明 |
| `AGENT-INSTRUCTIONS.md` | 更新版本号 |
| `v6.6-CHANGELOG.md` | 新建 |

## 四、Git 标签

- `v6.6-stable` — 本次修复的稳定回退点