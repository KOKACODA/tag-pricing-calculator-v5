# KOKALabel 报价系统 变更日志

## v9.1.0（2026-09-05）

### 合并安全加固（P0+P1）与 v9.0.1 数据基线

- 将此前在 v8 分支完成的安全加固并入 v9.0.1 数据基线：XSS 转义修复、导入校验加固、HTTP 响应头强化（P0）；数据防篡改签名 + 前端口令门（P1）。
- 1楼报价表为最新 **28 个报价组**（v9.0.1）；3楼 10 张保持不变。
- 全站版本号统一为 v9.1.0。

## v9.0.1（2026-09-04）

### 1楼报价表再次更新（29 组 → 28 组）

- 按最新附件 `KOKALabel1楼_20260904_1531(1).xlsx` 更新 1楼报价表：29 个报价组 → **28 个报价组**（全称编号 1楼小组-1~28 连续）。
- 各组含独立直接系数、批量报价、规格矩阵与工艺；**3楼 10 张保持不变**。
- 迁移条件更新为 `dataVersion=9.0.1`（`migrateV9ReplaceFloor1`），旧数据升级时保留 3楼（含用户自定义改价），仅替换 1楼。
- 纸张 id 重排 `paper2_1 ~ paper2_28`。

## v9.0.0（2026-09-04）

### 1楼报价表替换为最新 Excel（34 张 → 29 个报价组）

- 按附件 `KOKALabel1楼_20260904_1531.xlsx` 重建 1楼报价表：34 张纸 → **29 个报价组**（纸张按材质组合重新分组，如「白纹卡600/黑卡纸600/牛皮纸600/纹棉纸800」一个报价组涵盖多种材质）。
- 各组含独立直接系数、部分批量直接报价、规格矩阵与工艺。
- **3楼 10 张保持不变**（含用户在系统里的自定义改价，迁移时保留）。
- 1楼工艺按附件重建；新增 `migrateV9ReplaceFloor1` 迁移（`dataVersion=9.0`），旧 localStorage 数据平滑升级。
- 纸张 id 重排为 `paper2_1 ~ paper2_29`。

### 新增临时系数第二种算法显示

- 同时填写「临时毛利系数 + 邮费快速修改」时，除了原「(成本 − 原邮费 + 新邮费) × 系数」（邮费乘系数），新增并排显示 **「(成本 − 原邮费) × 系数 + 新邮费」（邮费不乘系数）**，用绿色实线卡片区分，方便两种报价口径对比。

### 版本号统一

- 所有文件版本号统一为 v9.0.0（含修正此前残留的 `v7.9`/`v7.8`/`8.1.0` 旧版本号）。

## v8.11.0（2026-09-04）

### 数据防篡改（P1）

- **导出完整性签名**：完整配置 / 本地备份导出时经 `signExport` 对数据体计算 `sha256Hex(canonicalJson(payload))` 写入 `meta.integrity`；导入时重算比对，篡改或损坏即阻断并提示；旧版无 `integrity` 字段的备份仍可恢复，并提示「无完整性校验」。
- **稳定序列化 + SHA-256**：`data.js` 新增纯 JS 同步实现 `canonicalJson`（对象键升序、数组保序、去空白）与 `sha256Hex`（无依赖，HTTPS 与离线 file:// 均可用）。

### 访问控制 · 前端口令门（P1）

- `index.html` 增加全屏口令遮罩，`app.js` 启动时校验 `sessionStorage["tagPricing_unlocked"]`，未解锁先要求口令（`sha256Hex(ACCESS_SALT + 口令)` 比对）再初始化界面；解锁仅写 sessionStorage（关页失效），不做持久免密。
- `data.js` 顶部新增 `ACCESS_ENABLED / ACCESS_SALT / ACCESS_PASSCODE_HASH` 占位（默认 `ACCESS_ENABLED=false` 关闭，附生成哈希与替换说明）。此门只防「打开直接看到」，真正保密需在 Cloudflare Access 开边缘鉴权。

### 测试

- 新增 `tests/integrity.test.mjs`（SHA-256 标准向量 + canonicalJson + 导出签名/导入校验回归），全量单测 18 项全绿。

## v8.10.0（2026-09-04）

### 安全加固（P0）

- **XSS 转义修复**：升级 `escapeHtml` 补齐双引号/单引号转义（同时适配属性值场景）；Excel 导入 / 吊绳导入 / 邮费导入 / 本地备份恢复等状态提示涉及的报价表名、纸张简称、工艺名、吊绳名、地区名、错误信息、文件名等动态内容全部经 `escapeHtml` 转义后再拼入 HTML，堵死存储型 XSS。
- **导入校验加固**：`parsePaperExcel` 的规格/工艺/直接系数/批量直接报价价格全部拒绝负数与 `NaN`/`Infinity`（新增 `isNonNegFinite`）；规格档位去重并升序排序，防止表头乱序/重复导致价格错位。
- **文件大小上限**：纸张/吊绳/邮费 Excel、完整配置、本地备份五个导入入口统一限制 50MB，防止超大文件导致卡死或内存耗尽。
- **Excel 公式注入防护**：导出 Excel 的名称/代码单元格（报价表名、纸张简称、规格代码、工艺名、吊绳名、地区名）经 `safeExcelText` 处理，`=`/`+`/`-`/`@` 开头自动加单引号转文本。
- **导入数据深校验**：`validateImportedData` 扩展为对报价表组/客户等级/纸张/吊绳/工艺/邮费/规格价格等全结构深校验（类型、非负、长度、数量上限），拦截恶意或损坏的备份 JSON。
- **响应头强化**：`_headers` 新增 `Strict-Transport-Security`（HSTS）与完整版 `Content-Security-Policy`（补 `base-uri`/`frame-ancestors`/`object-src`/`worker-src`/`font-src`/`form-action`）。

## 维护（2026-09-04 · 无版本号变更）

- 补全 `docs/main-branch-summary.md` 至**论文级完整历史档案**（约 280 行 → 1161 行）：从 git 历史恢复三份被删归档文档 `文档1-版本修改总记录.md`（297 行）/ `文档2-完整流程文档.md`（485 行）/ `文档3-大总结与技术分析.md`（233 行，共 1015 行），整合出 pre-v4.3 前史（v1.0 前身 2026-07-28 → v4.2.1 2026-08-05）逐版本明细 + 技术架构深度分析（五区代码结构 / 三级数据模型 / calculate 九步流程 / Excel 导入导出 / 存储状态 / 部署流程）+ 设计决策与技术债 + 版本演进方法论 + main↔v8 关系与历史查阅索引
- 修订该档案中 main 历史描述为全谱系「2026-07-28 起点 → v7.10 终点」，不再局限于 v4.3→v7.10

## 维护（2026-09-03 · 无版本号变更）

- 新增 `docs/HANDOFF-v8.md`（项目转手/交接文档），覆盖 v8.9.0 全貌、main↔v8 双分支关系、部署与维护注意事项，取代旧 HANDOFF-v5/v6
- 关联：`main` 分支同步完成旧文档清理（删除含 account_id 的 HANDOFF-v5 等冗余文档）并记录日志
- 新增根目录 `AGENTS.md`（新 agent 接手「只读入口」，约 60 行）：给出文件地图 + 行数 + 「按任务只读局部」决策、函数索引、验证命令，降低首次接手的 token 消耗
- 新增 `README.md`（v8 分支此前无 README，本次补齐）：项目定位 / 线上地址 / 功能 / 分支说明 / 结构 / 验证 / 文档索引
- 新增 `docs/main-branch-summary.md`（main 分支 v7.10 旧谱系超级详细历史档案）：版本时间线总览 + 逐版本详细变更（2026-07-28 起点 → v7.10 终点，含 pre-v4.3 前史）+ 架构/数据模型/安全/性能四大演进主线 + main↔v8 双分支关系 + 历史查阅操作索引，供课题研究与跨 agent 快速读取旧数据

## v8.9.0（2026-09-03）

### 新增「吊牌成本合计」

- 在结果区「邮费」行上方（邮费与成本合计之间）新增「吊牌成本合计」行。
- 公式：`吊牌成本合计 = 成本合计 − 邮费`（标准模式），即吊牌本身成本（纸张+工艺+吊绳，不含邮费运输）。
- 直接系数模式下 = 成本合计（本身就不含邮费）。
- 缺价时显示「部分缺价」。

## v8.8.0（2026-09-03）

### 修正默认报价隐藏行为

- 关闭「默认报价」开关时，邮费快速修改区的「修改邮费后报价」标签和 3 张客户等级卡片**正确隐藏**（v8.6 误把这三处都摘出来了）
- **「修改后成本」红字始终显示**，不受默认报价开关控制（这是你的硬性要求）
- 即：默认报价开关只控制「邮费标签 + 客户卡片 + 默认报价卡片」三处，「修改后成本」独立始终可见

## v8.7.0（2026-09-03）

### 输入临时系数时「修改后成本」保持显示

- 同时填写「临时毛利系数 + 邮费快速修改」时（v8.1 联动），原行为：邮费修改标签和 3 个客户等级卡片都隐藏，仅由临时系数卡片展示合并结果。
- 新行为：「修改后成本」红字**保持显示**，让用户能直接看到新成本是多少（再被临时系数相乘得到最终价）。
- 仅标签与 3 个客户等级卡片继续隐藏（避免与临时系数卡片重复）。

## v8.6.0（2026-09-03）

### 邮费快速修改区始终可见

- 邮费快速修改区（「修改邮费后报价」标签 / 「修改后成本」红字 / 报价卡片）不再随「默认报价」开关隐藏，始终保持可见。
- 仅「默认报价」标签与默认客户等级卡片受显隐开关控制。
- 同步修正资源引用缓存参数 `?v=8.6.0`。

## v8.5.0（2026-09-03）

### 修复空字符串价格 Bug（重要）

- **问题**：`data.js` 中纸张价格/直接系数有 106 处空字符串 `""`（v8.0 用 Excel 生成时，空单元格 `''` 未转 `null`），导致 `hasExactTier` 把 `Number("")===0` 误判为「价格 0」，部分档位（如 30000/100000/3000/4000/2500/7500）被按 0 元计价。
- **修复**：
  - 将 `data.js` 中 106 处空字符串统一改为 `null`（价格 80 处 + 直接系数 26 处）。
  - `hasExactTier` 增加 `v !== ""` 防御，避免未来 Excel 导入再引入空字符串误判。
- 验证：`paper2` 30000 档由「价格 0」修正为「无价格（缺价提示）」，`paper7` 直接系数空值修正为 `null`。

## v8.4.0（2026-09-03）

### 数据同步（纠正版 Excel）

- 用纠正版 Excel（`KOKALabel1楼.xlsx` / `KOKALabel3楼.xlsx`）作为最新权威数据源，重新确认纸张与工艺数据。
- 对比结果：纸张 44 张完全一致；3 楼工艺有 32 处「空单元格 vs null」的表示差异，统一为 `null` 后无实质价格变化。
- 新增 `migrateV8_4CorrectData` 迁移 + `dataVersion` 标记，首次升级时覆盖用户本地纸张/工艺数据为最新默认（此后用户自定义不再被覆盖）。
- 报价表显示名称保持「1楼」「3楼」。

## v8.3.0（2026-09-03）

### 邮费快速修改增强

- 修改邮费后新增「**修改后成本**」显示，红字高亮（与主结果区「成本合计」同风格）。
- 「修改后成本」= 原成本 − 原邮费 + 新邮费。
- 邮费快速修改区（标签 / 修改后成本 / 报价卡片）随「默认报价」开关联动：默认报价隐藏时一并隐藏，显示时恢复。

## v8.2.0（2026-09-03）

### Bug 修复（P1）

- **缺价不再生成正式报价**：标准报价的 `costKnown` 改为纳入全部缺价状态（`!hasMissingTier`），纸张/工艺/吊绳/邮费任一缺价时正确标记「部分缺价」，不再用 0 凑出成本生成报价。
- **面积 6000mm² 边界回落**：强制映射范围由 `5500–5999` 扩展为 `5500–6000`，6000 强制命中 `006`，不再回落到 `055` 导致价格下降。
- **邮费 Excel 导入持久化**：导入后写入 `localStorage`，刷新页面不再丢失。
- **快照恢复完整还原**：恢复快照时补上吊绳配置与邮费配置的还原（此前遗漏）。

### 其他优化

- **默认纸张真正生效**：计算器初始化纸张时优先使用个人主页设置的默认纸张（若存在于当前报价表）。
- **文案修正**：恢复默认提示「43 张」改为「44 张」，报价表名改为「1楼/3楼」；导出 JSON 版本字段 `6.5` 改为 `8.1.0`（本次提交后为 8.2.0）。

## v8.1.0（2026-09-03）

### 临时毛利系数与邮费快速修改联动

- 当**同时**填写「临时毛利系数」和「邮费快速修改」时，合并为一个最终价格：
  `最终价格 = (原成本 − 原邮费 + 新邮费) × 临时毛利系数`
- 此时隐藏独立的邮费修改卡片（3 个客户等级），仅显示合并后的临时系数卡片，标签带「新邮费」提示。
- 只填其中一个时，行为保持不变（临时系数 → 原成本 × 系数；邮费修改 → 新成本 × 客户等级系数）。

## v8.0.0（2026-09-03）

### 报价表结构重构

- 报价表从 4 个重构为 **2 个**：
  - **1楼**（`priceList1`）= 34 张纸（原 2 号 16 + 3 号 12 + 4 号 6）
  - **3楼**（`priceList2`）= 10 张纸（原 1 号）
- 小组名改为 **1楼小组 / 3楼小组**；报价表名改为 **1楼 / 3楼**。

### 纸张数据替换

- 按 Excel（`KOKALabel1号报价表`、`KOKALabel2号报价表`）覆盖全部 44 张纸的简称、折扣系数、直接系数、规格价格、批量直接报价。
- 纸张 id 保持不变，**工艺配置未替换**（保持原有数据）。
- 新增 `migrateV8Restructure` 迁移：检测旧 4 报价表结构后，自动覆盖纸张/报价表/邮费数据。

### 邮费重构

- 地区从 3 个扩展为 **9 个**。
- 档位改为 `500 / 1000 / 2000 / 5000 / 10000`（去掉 3000 档与「小面积 8 折」规则）。
- 新增「大于 10000」超量系数（`overTierCoeff`），计算逻辑分三种：
  - **固定档**：数量正好命中档位 → 直接查价；
  - **超量档**：数量 > 10000 → 弹对话框输入吊牌重量(kg)，邮费 = 重量 × 系数；
  - **中间档**：数量为 2500/3000/4000 等无定价档 → 弹对话框手动输入邮费。
- 新增两个输入对话框（重量 / 手动邮费），会话级缓存避免重复弹窗。
- 邮费的就地编辑、Excel 导入/导出/模板均适配新结构（`超量系数` 列替换原阈值/折扣列）。

### 其他

- 版本号 `v7.9.0` → `v8.0.0`。
