# KOKALabel报价系统 v6.5 — 版本总结文档

> 版本：v6.5 · 日期：2026-08-15
> 核心主题：**折扣只在标准报价触发，直接系数模式不打折**

---

## 一、v6.5 核心变更

### 1. 标准报价模式恢复纸张折扣

标准报价模式下，纸张价格 = 原价 × `paper.discount`（打折）：

- 40C棉麻布（discount 0.75）1000张：90 × 0.75 = 67.50 元
- 500白卡纸（discount 0.75）：原价 × 0.75
- 400铜版纸（discount 0.91）：原价 × 0.91

纸张折后价合计显示折后价合计，再 × 客户等级系数 = 最终报价。

### 2. 直接系数模式不打折

直接系数模式下，纸张始终用原价（不打折）：

- 40C棉麻布（无直接系数）1000张：显示原价 90 元
- 各纸张表格单价列显示原价
- 有直接系数的纸张：原价 × 直接系数 = 最终报价
- 无直接系数的纸张（回退）：原价 × 客户等级系数 = 最终报价

### 3. 计算逻辑

```javascript
// app.js calculate() 中
const baseUnitPrice = hasExactTier(spec.prices, tier)
  ? Number(spec.prices[tier]) * (isDirect ? 1 : paper.discount)
  : null;
```

- `isDirect = true`（直接系数模式）：乘 1，即原价（不打折）
- `isDirect = false`（标准报价模式）：乘 `paper.discount`（打折）

---

## 二、v6.5 涉及文件

| 文件 | 变更 |
|------|------|
| `js/app.js` | `calculate()` 中 `baseUnitPrice` 改为 `原价 × (isDirect ? 1 : paper.discount)`；版本号 v6.5 |
| `js/data.js` | 版本号 v6.5（数据结构不变） |
| `index.html` | 版本号 v6.5（title + CSS/JS 引用 `?v=6.5`） |
| `README.md` | 版本号与 v6.5 改动摘要更新 |
| `CHANGELOG.md` | 新增 v6.5 更新记录 |
| `HANDOFF-v6.0.md` | 版本号与计算方式说明更新 |
| `AGENT-INSTRUCTIONS.md` | 版本号更新 |

---

## 三、后续维护说明

1. **折扣逻辑**：折扣只在标准报价触发（`isDirect ? 1 : paper.discount`）。如需调整，修改 `calculate()` 中 `baseUnitPrice` 的计算。
2. **价格对比**：
   - 标准报价模式：40C棉麻布 1000张显示 67.50（折后价）
   - 直接系数模式：40C棉麻布 1000张显示 90（原价）
3. **版本号同步**：修改版本号时，index.html（title + CSS/JS 引用 `?v=X.X`）、app.js、data.js 需同步更新。
