# KOKALabel报价系统 v6.0 — 版本总结文档

> 更新日期：2026-08-13
> 仓库：KOKACODA/tag-pricing-calculator-v5
> 部署：https://tag-pricing-calculator-v5.pages.dev

---

## 一、v6.0 核心变更

### 1. 直接系数档位规则可在个人主页自由编辑

**变更前**：直接系数的批量档位规则（`DIRECT_COEFF_TIER_RULES`）为硬编码常量，用户无法在界面上修改，只能通过改代码调整。

**变更后**：
- `DIRECT_COEFF_TIER_RULES` 改为 `let` 变量，通过 localStorage 持久化
- 新增 `DEFAULT_DIRECT_COEFF_TIER_RULES` 常量保留出厂默认值
- 个人主页「直接系数设置」卡片内新增「批量档位系数规则」编辑区域
- 支持新增/删除/修改每条规则的张数范围和最高/最低系数
- 最大张数留空表示无上限（Infinity）
- 修改后实时保存并立即重算报价

### 2. 档位规则默认值

| 档位范围 | 最高系数 | 最低系数 | 说明 |
|---|---|---|---|
| 1000 ~ 4000 张 | 1.6 | 1.5 | 中间等级等差插值 |
| 5000 ~ 10000 张 | 1.5 | 1.45 | 中间等级等差插值 |
| 20000 张以上 | 1.45 | 1.4 | 无上限 |
| 500 张及以下 | 使用等级设置值 | 使用等级设置值 | 默认 1.5 / 1.45 / 1.4 |

### 3. 导出/导入备份集成

- 本地备份导出 JSON 现包含 `directCoeffLevels` 和 `directCoeffTierRules`
- 本地备份导入时自动恢复档位规则
- 「一键恢复全局默认」包含档位规则重置

---

## 二、v5.7 ~ v6.0 完整功能变更记录

### v5.7 — 直接系数计算模式
- 参数输入旁新增模式切换器（标准报价 / 直接系数计算）
- 直接系数模式：不计算吊绳和邮费，成本 = 纸张 + 工艺
- 个人主页新增独立直接系数等级设置（默认 1.5 / 1.45 / 1.4）
- 临时毛利系数标签自动切换为临时直接系数
- 隐藏邮费快速修改栏和吊绳/邮费结果行

### v5.8 — 代码手动切换功能
- 报价结果表格代码列新增上下箭头按钮
- 点击上/下切换到对应纸张的不同代码规格
- 手动切换后显示蓝色「手动」标签
- 切换纸张或尺寸时自动恢复自动匹配

### v5.9 — 直接系数按批量档位自动调整
- 新增 `DIRECT_COEFF_TIER_RULES` 批量档位系数规则
- 直接系数模式下系数根据批量档位自动调整
- 报价卡片右上角显示系数徽章（×1.6 等）
- 个人主页直接系数说明区域增加档位规则说明

### v6.0 — 档位规则可编辑
- 档位规则从常量改为可持久化变量
- 个人主页新增档位规则编辑器
- 导出/导入/重置功能全面集成档位规则

---

## 三、技术实现要点

### 数据结构

```javascript
// data.js
const DEFAULT_DIRECT_COEFF_TIER_RULES = [
  { tierMin: 1000,  tierMax: 4000,      max: 1.6,  min: 1.5  },
  { tierMin: 5000,  tierMax: 10000,     max: 1.5,  min: 1.45 },
  { tierMin: 20000, tierMax: Infinity,  max: 1.45, min: 1.4  }
];
let DIRECT_COEFF_TIER_RULES = loadFromStorage("directCoeffTierRules",
  DEFAULT_DIRECT_COEFF_TIER_RULES.map(r => ({ ...r })));
```

### 核心函数

```javascript
// app.js - 根据档位等差插值计算各级系数
function getDirectCoeffsForTier(tier) {
  const levels = DIRECT_COEFF_LEVELS;
  const rule = DIRECT_COEFF_TIER_RULES.find(r =>
    tier >= r.tierMin && tier <= r.tierMax
  );
  if (!rule) return levels.map(l => ({ ...l }));

  const n = levels.length;
  if (n <= 1) return [{ ...levels[0], coefficient: rule.max }];

  const step = (rule.max - rule.min) / (n - 1);
  return levels.map((l, i) => ({
    ...l,
    coefficient: Math.round((rule.max - step * i) * 100) / 100
  }));
}
```

### localStorage 键

| 键名 | 用途 | 默认值 |
|---|---|---|
| `tagPricing_directCoeffLevels` | 直接系数等级列表 | 1.5 / 1.45 / 1.4 |
| `tagPricing_directCoeffTierRules` | 档位系数规则 | 3 条默认规则 |

---

## 四、文件变更清单

| 文件 | 变更类型 | 说明 |
|---|---|---|
| `js/data.js` | 修改 | 新增 `DEFAULT_DIRECT_COEFF_TIER_RULES`，`DIRECT_COEFF_TIER_RULES` 改为 let + loadFromStorage |
| `js/app.js` | 修改 | 新增 `renderDirectTierRules/addTierRule/resetTierRules`，集成导出/导入/重置 |
| `index.html` | 修改 | 直接系数设置卡片内新增档位规则编辑区域 |
| `css/style.css` | 修改 | 新增 `.tier-rule-editor` 系列样式 |

---

## 五、备份与版本标签

| 标签 | 对应版本 | 说明 |
|---|---|---|
| `v5.2.1-stable` | v5.2.1 | 30+ Sheet 修复 + 面积系数前稳定版 |
| `v5.7-stable` | v5.7 | 直接系数模式前稳定版 |
| `v5.8-stable` | v5.8 | 代码切换功能前稳定版 |
| `v5.9-stable` | v5.9 | 档位规则编辑前稳定版 |
| `backup/v5.2.1` | 分支 | v5.2.1 完整备份分支 |

---

## 六、已知限制与未来方向

1. **档位规则冲突检测**：当前不校验多条规则的张数范围是否重叠，用户需自行确保不重叠
2. **等级数量变化时的插值**：删除/新增直接系数等级后，插值逻辑自动适配，但用户需理解中间等级是等差分布
3. **标准报价模式不受影响**：档位规则仅在直接系数模式下生效，标准报价模式仍使用毛利系数
