# KOKALabel 报价系统 · 部署与运维日志

> 记录版本上线、数据更新、部署状态。敏感信息（账号/密码/密钥/令牌）不写入本文件，统一存于仓库外凭据备份。

## 2026-09-05

### v9.4.2 main 同步 + 历史档案 + 目录规范 ✅

- `main` 分支 force 同步到 `v8`（旧 v7.10 谱系经本地备份 tag `archive/v7.10-main` 保留）。
- 新增 `docs/项目历史与技术总档案.md`（全量历史 + 技术 + 转手方案）；`README` / `AGENTS` 分支说明同步修正。
- 全站版本号 v9.4.2。
- 部署：`wrangler pages deploy` 直传已上线（2026-09-06），生产域名 https://tag-pricing-calculator-v5.pages.dev 标题确认 v9.4.2；提交 `21a53bc` 已推送 `v8` 与 `main`，并打 tag `v9.4.2`。

### v9.4.1 快照与统计报表合并框体 + 导出 + 清除缓存 ✅

- 「本地快照列表」与「统计报表」合并为独立卡片「快照与统计报表」，置于个人主页「数据管理」下方；「本地快照」标签更名「价格配置」；统计报表由弹窗改为内联渲染。
- 新增「导出快照」（签名 JSON）与「导出统计报表」（XLSX 三工作表）；均本机下载、不上传。
- 新增「一键恢复默认并清除缓存」（删除报价历史 + 本地快照）；保留原「一键恢复全局默认设置」（不影响历史与快照）。
- 部署：`wrangler pages deploy` 直传（`--branch main`）；推 GitHub 后需手动部署（Git Provider = No）。

### v9.4.0 统计报表（本地，不上传）✅

- 定位为 v9.4 版本（区别于 v9.1 数据基线）：「更多功能 → 统计报表」占位卡片启用，打开独立统计框体。
- 三项指标：月度报价次数、热销尺寸（宽×长）、利润分布（建议报价 − 成本）。
- 数据来源：本机 `localStorage` 已保存报价历史；纯前端 CSS 条形图，零第三方 CDN，符合 CSP `script-src 'self'` 与离线双击使用；不上传云端。
- 部署方式：`wrangler pages deploy` 直传；推 GitHub 后需手动部署（本项目为直传项目，Git Provider = No）。
- 结果：提交 `1277292` 已推送 `v8`，并已直传上线；生产域名 https://tag-pricing-calculator-v5.pages.dev 标题确认 **v9.4.0**。

### v9.1.0 生产部署完成 ✅

- 生产域名 https://tag-pricing-calculator-v5.pages.dev 已切换至 **v9.1.0**（`index.html` 标题与 `js/data.js` 版本头均确认 v9.1.0）。
- 内容：1楼最新 **28 个报价组**（v9.0.1 数据基线）＋ P0/P1 安全加固（XSS 转义 / 导入校验 / 响应头 / 数据防篡改签名 + 前端口令门）。
- 部署方式：`wrangler pages deploy` 直传（`--branch main` 生产分支）。
- 根因说明：项目 `tag-pricing-calculator-v5` 为**直传项目（Git Provider = No）**，推 GitHub 不会自动触发部署；生产分支为 `main`。
- Cloudflare 账户：`KOKA`（account_id `a4fdb353836f801c65072d7810fbcc32`）；本次令牌关联邮箱 `2996978242@qq.com`。

### 后端保留说明（重要）

- Master 明确：「不要后端」= **不使用**，并非删除。
- 后端源码 v9.2 / v9.3（`functions/` 账号系统、`login.html`、`js/auth.js` 等）**未删除、未部署、未启用**，仍完整保留于 `/data/user/work/v9_full/V9/{9.2,9.3}`。
- 已另做版本备份压缩包：`/workspace/KOKALabel-后端版本备份-v9.2-v9.3.tar.gz`（964KB，含 9.2/9.3 全部源码）。

### 版本与数据

- 代码基线：GitHub `KOKACODA/tag-pricing-calculator-v5` · 分支 `v8`。
  - `bac88c4`：合并 v9.0.1 数据（28 报价组）与安全加固 → v9.1.0。
  - `ae1c301`：新增本部署日志。
- 价格数据：与 v9.2.0 / v9.3.0 的 `data.js` 逐行比对一致（9.2/9.3 仅新增授权/后端账号能力，无价格变化）。

### 凭据

- 后端管理员初始凭据 + SETUP_KEY、部署基准账号：存于仓库外 `/workspace/KOKALabel-凭据备份-勿提交.md`，严禁入库。
- 本次 Cloudflare 部署令牌仅临时使用，未落盘。