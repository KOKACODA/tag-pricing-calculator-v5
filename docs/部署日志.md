# KOKALabel 报价系统 · 部署与运维日志

> 记录版本上线、数据更新、部署状态。敏感信息（账号/密码/密钥）不写入本文件，统一存于仓库外的凭据备份。

## 2026-09-05

### v9.1.0 推送 GitHub（数据更新完成）

- 分支：`v8`（仓库默认分支，当前线上分支）
- 提交：`bac88c4` `merge: 合并 v9.0.1 数据(1楼28报价组)与 v8 安全加固 → v9.1.0`
- 内容：1楼报价表为最新 **28 个报价组**（v9.0.1 数据基线）＋ P0/P1 安全加固（XSS 转义 / 导入校验 / 响应头 / 数据防篡改签名 + 前端口令门）。
- 全站版本号统一为 v9.1.0。
- 价格数据已验证：与 v9.2.0 / v9.3.0 的 `data.js` 逐行比对，报价数据一致（9.2/9.3 仅新增授权/后端账号等能力，无价格变化）。

### 部署状态（待处理）

- Cloudflare Pages 项目名：`tag-pricing-calculator-v5`，静态托管（build_command 空，destination_dir `/`），Git 集成监听 `v8` 分支。
- 推送后 webhook **未自动触发**，线上仍停留 `v8.9.0`（`data.js` 头仍 v7.9）。
- 待办：在 Cloudflare 后台重新触发部署，或提供 CF API Token（Pages Edit 权限）＋ account_id 以用 `wrangler pages deploy` 直传。

### 凭据与账号基线

- 最新部署基准账号：`Koka2996978242@outlook.com`（账户不一致是临时测试所致，以此为准）。
- 后端管理员初始凭据已由 Master 本人在仓库外备份，不乱写入仓库。

### 运维动作记录

- 已移除重复解压临时目录、清理 `v9src` 本地 remote 与 `/tmp` 中间文件。
- 保留 `v9_full`（含 v9.3 后端 `functions/`）作为未来后端上线参考源。